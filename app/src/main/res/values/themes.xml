<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.EmotionDiary" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>

        <!-- Text for title -->
        <item name="android:textColorPrimary">@color/colorOnPrimary</item> <!-- 대부분 이걸로 적용됨 -->
        <item name="titleTextColor">@color/colorOnPrimary</item> <!-- 일부 기기에서 필요 -->
        <item name="toolbarStyle">@style/CustomToolBar</item>

        <!-- Secondary / Accent -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@color/colorSecondaryVariant</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>
        <item name="colorAccent">@color/colorSecondary</item>

        <!-- Background & Surface -->
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorOnBackground">@color/colorOnBackground</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>

        <!-- Error -->
        <item name="colorError">@color/colorError</item>
        <item name="colorOnError">@color/colorOnError</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/colorPrimaryVariant</item>

        <!-- Custom font styles -->
        <item name="android:textViewStyle">@style/customTextViewFontStyle</item>
        <item name="android:buttonStyle">@style/customButtonFontStyle</item>
        <item name="android:editTextStyle">@style/customEditTextFontStyle</item>
        <item name="android:radioButtonStyle">@style/customRadioButtonFontStyle</item>
        <item name="android:checkboxStyle">@style/customCheckboxFontStyle</item>
    </style>

    <style name="SplashTheme" parent="Theme.EmotionDiary">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="CustomToolBar" parent="@style/Widget.AppCompat.Toolbar">
        <item name="titleTextColor">@color/colorOnPrimary</item>
    </style>
</resources>
